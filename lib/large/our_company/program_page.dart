import 'package:flutter/material.dart';

class PartnerProgram extends StatelessWidget {
  const PartnerProgram({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
